{
  "name": "weather-share",
  "version": "1.0.0",
  "private": true,
  "description": "SaxWeather share-link Worker — rich iMessage previews via Open Graph",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler check"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
